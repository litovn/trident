# AIGoat L0 — obfuscated prompt injection smoke (TRD-PRM-004).
# Exercises the real PyRIT converter pipeline (UnicodeConfusable) end-to-end.
#
# Per ADR-008 the manifest declares *constraints* (safety, hosts, budget), not the plan.
# Layer and technique selection are inferred from the --prompt by the ranker.
# Pinning fields (`layers`, `technique_allowlist`) are intentionally omitted here
# so TRIDENT is free to decide. `technique_denylist` is kept as a placeholder
# (refined enforcement TBD).
campaign_id: aigoat-l0-prompt-obfuscated
mode: attack
target_profile_id: aigoat
technique_denylist: []
host_allowlist: [127.0.0.1]
query_budget_per_vertical: 5
hitl_techniques: []
egress_local_only: true
