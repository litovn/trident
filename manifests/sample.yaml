# TRIDENT manifest — recon smoke against the in-process echo target.
# Mode = recon → only phase ∈ {recon, both} survives the gate; exploit-only
# techniques are blocked at runtime and logged as `rule=mode_intent`.
campaign_id: smoke-001-recon
mode: recon
target_profile_id: echo
technique_denylist: []
host_allowlist: [memory://echo, 127.0.0.1]
query_budget_per_vertical: 20
hitl_techniques: []
