# AIGoat L0 — direct prompt injection smoke run.
# Layer + technique selection are driven by the --prompt via the ranker (ADR-008);
# keep the prompt specific (e.g. mention direct prompt injection / TRD-PRM-002).
campaign_id: aigoat
mode: attack
target_profile_id: aigoat
technique_denylist: []
host_allowlist: [127.0.0.1]
query_budget_per_vertical: 5
hitl_techniques: []
